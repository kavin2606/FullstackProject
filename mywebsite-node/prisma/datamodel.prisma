type Item {
  id: ID! @id
  createdAt: DateTime! @createdAt
  name: String!
  description: String!
  category: Category
  quantity: String!
  comment: String!
  rating: String!
  postedBy: User
}
type User {
  id: ID! @id
  name: String!
  email: String! @unique
  password: String!
  items: [Item!]!
}

type Category {
  id: ID! @id
  name: String! @unique
  itemslist: [Item!]!
}
