type Query {
  info: String!
  feed(filter: String): [Item!]!
  usersorders: User!
}

type Mutation {
  post(name: String!,description: String!,category: String! ,quantity: String!,comment: String!,rating: String!): Item!
  signup(email: String!, password: String!, name: String!): AuthPayload
  login(email: String!, password: String!): AuthPayload
  addcategory(name:String!): Category
  placeorder(id:String!): Order
}

type Item {
  id: ID!
  name: String!
  description: String!
  quantity: String!
  comment: String!
  rating: String!
  postedBy: User
  category: Category
  orderinfo: [Order!]!
}

type AuthPayload {
  token: String
  user: User
}

type User {
  id: ID!
  name: String!
  email: String!
  items: [Item!]!
  orderdetails: [Order!]!
}

type Category {
  id: ID!
  name: String!
  itemslist:[Item!]!
}

type Order {
  id: ID!
  itempurchased: Item!
  orderedby: User!
}
